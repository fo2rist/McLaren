<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/margin_two"
    android:paddingRight="@dimen/margin_two"
    >

    <TextView
        android:id="@+id/title_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="160dp"
        app:layout_constraintTop_toTopOf="parent"
        android:paddingTop="@dimen/margin_one"
        android:paddingBottom="@dimen/margin_one"
        android:background="@android:color/white"
        style="@style/TextTransmissionTitle"
        android:gravity="center"
        tools:text="Monaco Grand Prix"
        />
    <android.support.v7.widget.RecyclerView
        android:id="@+id/transmission_list"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/title_text"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:background="@android:color/white"
        tools:listitem="@layout/item_transmission"
        />
    <TextView
        android:id="@+id/empty_list_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/message_no_transmission"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="@id/transmission_list"
        app:layout_constraintTop_toTopOf="@id/transmission_list"
        app:layout_constraintRight_toRightOf="@id/transmission_list"
        app:layout_constraintRight_toLeftOf="@id/transmission_list"
        android:visibility="gone"
        style="@style/TextTransmissionSubtitle"
        tools:visibility="visible"
        />
    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        style="@style/Base.Widget.AppCompat.ProgressBar.Horizontal"
        android:indeterminate="true"
        android:visibility="invisible"
        tools:visibility="visible"
        />
</android.support.constraint.ConstraintLayout>
