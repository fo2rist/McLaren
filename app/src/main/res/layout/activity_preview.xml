<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".ui.previewscreen.PreviewActivity"
    >
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay"
        >
        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsing_toolbar_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/background_main"
            app:contentScrim="@drawable/background_main"
            app:layout_scrollFlags="scroll|enterAlways">
        <RelativeLayout
            android:id="@+id/header_images_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            >
            <com.github.fo2rist.mclaren.ui.widgets.SafeViewPager
                android:id="@+id/header_image_pager"
                android:layout_width="match_parent"
                android:layout_height="@dimen/preview_header_height"
                android:layout_gravity="center_horizontal"
                android:scaleType="centerCrop"
                android:contentDescription="@null"
                android:visibility="gone"
                app:layout_collapseMode="parallax"
                tools:visibility="visible"
                tools:src="@drawable/driver_turvey"
                />
            <me.relex.circleindicator.CircleIndicator
                android:id="@+id/dot_indicator"
                android:layout_width="match_parent"
                android:layout_height="@dimen/margin_five"
                android:layout_alignParentBottom="true"
                />
        </RelativeLayout>
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@null"
            app:popupTheme="@style/AppTheme.PopupOverlay"
            app:layout_collapseMode="pin"
            />
        </com.google.android.material.appbar.CollapsingToolbarLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        >
    </FrameLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
